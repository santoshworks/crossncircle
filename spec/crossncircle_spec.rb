require "spec_helper"

RSpec.describe Crossncircle do
  it "has a version number" do
    expect(Crossncircle::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
